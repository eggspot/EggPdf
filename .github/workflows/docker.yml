name: Docker

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Build and push Service image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.service
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            eggspot/eggpdf-service:latest
            eggspot/eggpdf-service:${{ steps.version.outputs.version }}
            ghcr.io/eggspot/eggpdf-service:latest
            ghcr.io/eggspot/eggpdf-service:${{ steps.version.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push CLI image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.cli
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            eggspot/eggpdf-cli:latest
            eggspot/eggpdf-cli:${{ steps.version.outputs.version }}
            ghcr.io/eggspot/eggpdf-cli:latest
            ghcr.io/eggspot/eggpdf-cli:${{ steps.version.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
